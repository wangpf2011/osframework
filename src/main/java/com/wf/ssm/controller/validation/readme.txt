在pojo中定义校验规则，而pojo是被多个controller所公用，当不同的controller方法对同一个pojo进行校验，但是每个controller方法需要不同的校验。

解决方法：
定义多个校验分组（其实就是一个java接口，接口中不需要定义任何方法，仅是对不同的校验规则进行分组），分组中定义有哪些规则，
每个controller方法使用不同的校验规则，
在pojo定义校验规则中，添加分组，说明此规则属于哪个分组。